# Runtime dependencies for the Quareia Companion backend.
# Pin to known-good minor versions; ``pip install -r requirements.txt``.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
SQLAlchemy>=2.0,<3.0
alembic>=1.13,<2.0
pydantic>=2.6,<3.0
email-validator>=2.0,<3.0

# Dev / test only (not required at runtime):
# pytest>=8.0
# httpx>=0.27
